{
  "$schema": "https://schema.tauri.app/capabilities/2",
  "identifier": "default",
  "description": "Default capabilities for the AllMyStuff GUI (the main window and every per-machine console, terminal and files window, each room call window, each video-popout window, and the CEC Console window)",
  "windows": [
    "main",
    "console-*",
    "terminal-*",
    "files-*",
    "room-*",
    "video-*",
    "cec-console"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "core:window:allow-close",
    "core:window:allow-destroy",
    "core:window:allow-set-fullscreen",
    "core:window:allow-is-fullscreen",
    "core:window:allow-set-focus",
    "shell:allow-open",
    "dialog:default",
    "clipboard-manager:allow-read-text",
    "clipboard-manager:allow-write-text"
  ]
}
