name: Release

# Triggered by the immutable `v*` tag that `just release X.Y.Z` pushes.
# workflow_dispatch is only an escape hatch for rebuilding an existing tag.
on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to build (e.g. v0.2.0)"
        required: true

permissions:
  contents: write

jobs:
  bundles:
    name: Build (${{ matrix.platform.name }})
    strategy:
      fail-fast: false
      matrix:
        platform:
          # Linux builds are pinned to Ubuntu 22.04 (glibc 2.35) — not
          # ubuntu-latest — so the resulting binaries run on Ubuntu 22.04+,
          # Debian 12, and other distros still on glibc 2.35/2.36/2.38.
          # Building on 24.04 links against glibc 2.39 and breaks anything
          # older with "GLIBC_2.39 not found". Matches MyOwnMesh /
          # MyOwnLLM so users get the same portability promise across apps.
          # Linux x64 runs in the qualified local linux-bundles job below.
          - { name: linux-aarch64,  os: ubuntu-22.04-arm, target: aarch64-unknown-linux-gnu, bundle_args: "" }
          - { name: macos-aarch64,  os: macos-14,         target: aarch64-apple-darwin, bundle_args: "" }
          - { name: macos-x86_64,   os: macos-14,         target: x86_64-apple-darwin, bundle_args: "" }
          # NSIS is the supported Windows installer: its install/uninstall
          # hooks provision the privileged interactive host. Do not also ship
          # an MSI that silently omits that required setup.
          # Windows runs in the qualified local windows-bundles job below.
    runs-on: ${{ matrix.platform.os }}
    # The Opus bundled by `audiopus_sys` (via the node engine + GUI `opus` dep)
    # declares a pre-3.5 `cmake_minimum_required` that CMake 4.x rejects; this
    # shim re-enables the old policy floor for every CMake build in the job.
    env:
      CMAKE_POLICY_VERSION_MINIMUM: "3.5"
      # Bake the release public key into every binary so the self-updater
      # requires a valid minisign signature (set the ALLMYSTUFF_RELEASE_PUBKEY
      # repo *variable* — it isn't secret — to enable; empty leaves the updater
      # SHA-256-only). See RELEASE-SIGNING.md.
      ALLMYSTUFF_RELEASE_PUBKEY: ${{ vars.ALLMYSTUFF_RELEASE_PUBKEY }}
      # Development/compile-only jobs may fall back to a zero-byte sidecar
      # placeholder, but a published app must physically contain the daemon
      # selected by .myownmesh-rev. This is build-time only; it adds no work
      # to the installed app.
      ALLMYSTUFF_REQUIRE_PINNED_MESH: "1"
    steps:
      - uses: actions/checkout@v4
        with:
          # A manual rebuild must compile the requested immutable release,
          # not whichever commit happens to be at the tip of the dispatching
          # branch. Tag pushes already resolve github.ref to the same tag.
          ref: ${{ inputs.tag || github.ref }}

      # Guard against the "tag says v0.2.0, binary reports 0.1.5" trap. The
      # CLI binary embeds CARGO_PKG_VERSION (via [workspace.package].version)
      # at compile time, the self-updater compares it against the release
      # feed, and the GUI's window title pulls from gui/src-tauri/Cargo.toml.
      # If any manifest disagrees with the tag, every install will misreport
      # itself and the self-update poll will loop forever. The workspace
      # Cargo.toml is the source of truth; scripts/bump-version.sh keeps the
      # GUI sub-workspace files in sync.
      - name: Verify tag matches manifest versions
        shell: bash
        run: |
          set -e
          TAG="${{ inputs.tag || github.ref_name }}"
          EXPECTED="${TAG#v}"
          fail() { echo "::error::$*"; exit 1; }
          got_workspace=$(awk '
            /^\[workspace\.package\]/ {in_pkg=1; next}
            /^\[/ {in_pkg=0}
            in_pkg && /^version =/ {gsub(/[ "]/, "", $0); split($0, a, "="); print a[2]; exit}
          ' Cargo.toml)
          got_gui_cargo=$(awk -F\" '/^version = / {print $2; exit}' gui/src-tauri/Cargo.toml)
          got_gui_pkg=$(node -e "console.log(require('./gui/package.json').version)")
          [[ "$got_workspace" == "$EXPECTED" ]] || fail "Cargo.toml [workspace.package].version is $got_workspace, expected $EXPECTED (tag $TAG). Run 'just release $EXPECTED' to bump."
          [[ "$got_gui_cargo" == "$EXPECTED" ]] || fail "gui/src-tauri/Cargo.toml version is $got_gui_cargo, expected $EXPECTED (tag $TAG). Run 'just release $EXPECTED' to bump."
          [[ "$got_gui_pkg"   == "$EXPECTED" ]] || fail "gui/package.json version is $got_gui_pkg, expected $EXPECTED (tag $TAG). Run 'just release $EXPECTED' to bump."
          echo "Manifest versions match tag $TAG ($EXPECTED)."

      # Mobile embeds MyOwnMesh as Rust git dependencies while desktop/node
      # consume the same release as a sidecar. Keep its committed lock on the
      # exact `.myownmesh-rev` tag so one platform cannot silently ship an
      # older mesh implementation.
      - name: Verify suite pins match the committed locks
        shell: bash
        run: bash ./scripts/sync-pinned-locks.sh --check

      # The Tauri GUI links the webkit/GTK stack, and both it and the headless
      # node binary link the media stack (ALSA via cpal, PipeWire/gbm via xcap,
      # xkbcommon via enigo). The bare CLI needs none of it, but installing
      # everything here covers all three build steps in one place.
      - name: Linux deps
        if: runner.os == 'Linux'
        timeout-minutes: 20
        run: |
          set -euo pipefail
          # Ubuntu 22.04 ships PipeWire 0.3.48, whose headers are too old
          # for the libspa 0.9 bindings xcap's screen capture pulls in —
          # bindgen generates spa_video_info_raw without the `flags` field
          # and libspa fails to compile (this is what sank the linux jobs
          # of the v0.1.1 release). The pipewire-upstream PPA publishes
          # current PipeWire built *for* 22.04, so the release keeps its
          # glibc 2.35 floor while the capture stack compiles. ci.yml
          # doesn't need this — it runs on ubuntu-latest (24.04), whose
          # stock headers are new enough; that asymmetry is why CI stayed
          # green while the release broke.
          # GitHub's hosted x86_64 image puts an Azure mirror first in this
          # mirror list. When that endpoint stops responding, apt can hang on
          # one Packages file indefinitely instead of reaching the official
          # fallback. Keep the fallback and remove only the unhealthy mirror.
          if [[ -f /etc/apt/apt-mirrors.txt ]] && \
             grep -q 'azure\.archive\.ubuntu\.com' /etc/apt/apt-mirrors.txt; then
            sudo sed -i '/azure\.archive\.ubuntu\.com/d' /etc/apt/apt-mirrors.txt
          fi

          # add-apt-repository refreshes every apt source by default. Suppress
          # that implicit, unbounded refresh and perform one explicit update
          # with retries and transport timeouts instead.
          sudo add-apt-repository -y --no-update ppa:pipewire-debian/pipewire-upstream
          apt_options=(
            -o Acquire::Retries=5
            -o Acquire::http::Timeout=30
            -o Acquire::https::Timeout=30
          )
          sudo apt-get "${apt_options[@]}" update
          sudo env DEBIAN_FRONTEND=noninteractive \
            apt-get "${apt_options[@]}" install -y \
            libwebkit2gtk-4.1-dev libgtk-3-dev libayatana-appindicator3-dev \
            librsvg2-dev libssl-dev libasound2-dev xdg-utils \
            libpipewire-0.3-dev libxkbcommon-dev libgbm-dev

      # Match the repo's rust-toolchain.toml (channel = "stable") and ci.yml
      # so the cross-compile target is added to the *same* toolchain cargo
      # actually uses. Pinning a different version here (e.g. @1.88.0) adds
      # the target to a toolchain rust-toolchain.toml then overrides, so the
      # non-native target (x86_64-apple-darwin on the arm64 macOS runner)
      # goes missing — "can't find crate for `core`".
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform.target }}

      # Two separate Cargo workspaces in this repo — the library/CLI
      # workspace at the root, and the Tauri GUI under `gui/src-tauri`. Each
      # gets its own cache scope so they don't stomp each other.
      - name: Cache (library workspace)
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: . -> target
          key: lib-${{ matrix.platform.os }}

      - name: Cache (GUI workspace)
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: gui/src-tauri -> target
          key: gui-${{ matrix.platform.os }}

      - name: Cache (node workspace)
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: node -> target
          key: node-${{ matrix.platform.os }}

      # No `version:` — pnpm/action-setup@v4 reads the version from
      # gui/package.json's `packageManager` field. Pinning a different
      # `version:` here causes pnpm 10+ to refuse to run with
      # ERR_PNPM_BAD_PM_VERSION. There is no root package.json in this repo,
      # so point the action at the GUI manifest explicitly — otherwise it
      # fails with "No pnpm version is specified".
      - uses: pnpm/action-setup@v4
        with:
          package_json_file: gui/package.json

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: gui/pnpm-lock.yaml

      - name: Install GUI dependencies
        working-directory: gui
        run: pnpm install --frozen-lockfile

      # Build the headless CLI first. The bundle step below ships it
      # alongside the GUI installer so users get a `scan`/`capabilities`
      # binary too, and the self-updater can fetch it as its own asset.
      - name: Build CLI
        run: cargo build --release --bin allmystuff --target ${{ matrix.platform.target }}

      # Build `amst` (AMSTerm) — the standalone mesh terminal. A separate,
      # self-contained program in the same root workspace (no webview, no media
      # stack), shipped as its own portable asset and installed by its own
      # `install-amst` script.
      - name: Build amst (AMSTerm)
        run: cargo build --release --bin amst --target ${{ matrix.platform.target }}

      # Build the headless node binary (`allmystuff-serve`, what `allmystuff
      # serve` runs). It carries the whole media/mesh engine, so it links the
      # same native stack as the GUI (minus the webview). Shipped alongside
      # the CLI so a headless `allmystuff serve` / `allmystuff service` works
      # without the desktop app.
      - name: Build node serve binary
        run: cargo build --release --manifest-path node/Cargo.toml --bin allmystuff-serve --target ${{ matrix.platform.target }}

      # Leave tauri-action a single, unambiguous app. `gui/mobile` is a second,
      # self-contained Tauri project (its own `[workspace]`, its own
      # tauri.conf.json) that shares `projectPath: gui`. tauri-action discovers
      # it and mis-resolves the desktop bundle to it — reading its stale version
      # and its empty `gui/mobile/target`, then failing "No artifacts were found"
      # even though the desktop bundles built fine under gui/src-tauri (this is
      # what broke every release after gui/mobile landed; v0.2.19 was the last
      # green one, before it existed). The desktop release never builds mobile,
      # and gui/src-tauri doesn't depend on it, so drop it from this ephemeral
      # checkout. Runs after `pnpm install`, so nothing that needs it has run.
      - name: Isolate the desktop app from the mobile Tauri project
        shell: bash
        run: rm -rf gui/mobile

      # Build the Tauri GUI bundle. tauri-action runs `pnpm tauri build`
      # under the hood and uploads the resulting installers (.deb /
      # .AppImage / .dmg / .msi / .exe) to the named release. `projectPath:
      # gui` points it at the GUI's own workspace. The GUI's build.rs
      # bundles the `myownmesh` daemon sidecar for the rev pinned in
      # .myownmesh-rev — so we deliberately do NOT set
      # ALLMYSTUFF_SKIP_SIDECAR here; the released app ships with mesh
      # built in.
      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          projectPath: gui
          tagName: ${{ inputs.tag || github.ref_name }}
          releaseName: ${{ inputs.tag || github.ref_name }}
          releaseDraft: false
          prerelease: false
          args: --target ${{ matrix.platform.target }} ${{ matrix.platform.bundle_args }}

      # Package the portable CLI binary as a tarball / zip so users who
      # don't want the GUI installer can `curl | tar` it, and the
      # self-updater can fetch `allmystuff-<platform>.<ext>` directly. The
      # .sha256 sidecar is what `allmystuff-updater` verifies against.
      - name: Package portable CLI binary
        shell: bash
        run: |
          set -e
          BIN=target/${{ matrix.platform.target }}/release/allmystuff
          if [[ "${{ runner.os }}" == "Windows" ]]; then BIN="${BIN}.exe"; fi
          if [[ ! -f "$BIN" ]]; then
            echo "CLI binary not found at $BIN"; exit 1
          fi
          mkdir -p dist-bin
          NAME="allmystuff-${{ matrix.platform.name }}"
          if [[ "${{ runner.os }}" == "Windows" ]]; then
            cp "$BIN" "dist-bin/allmystuff.exe"
            (cd dist-bin && 7z a "${NAME}.zip" allmystuff.exe)
            (cd dist-bin && (sha256sum "${NAME}.zip" || shasum -a 256 "${NAME}.zip") > "${NAME}.zip.sha256")
          else
            cp "$BIN" "dist-bin/allmystuff"
            tar -czf "dist-bin/${NAME}.tar.gz" -C dist-bin allmystuff
            (cd dist-bin && (sha256sum "${NAME}.tar.gz" || shasum -a 256 "${NAME}.tar.gz") > "${NAME}.tar.gz.sha256")
          fi

      # Package the portable `amst` (AMSTerm) binary so `install-amst` can fetch
      # `amst-<platform>.<ext>` directly. The raw binary is left in dist-bin
      # (the upload glob only matches the archives + .sha256, like the CLI step).
      - name: Package portable amst binary
        shell: bash
        run: |
          set -e
          BIN=target/${{ matrix.platform.target }}/release/amst
          if [[ "${{ runner.os }}" == "Windows" ]]; then BIN="${BIN}.exe"; fi
          if [[ ! -f "$BIN" ]]; then
            echo "amst binary not found at $BIN"; exit 1
          fi
          mkdir -p dist-bin
          NAME="amst-${{ matrix.platform.name }}"
          if [[ "${{ runner.os }}" == "Windows" ]]; then
            cp "$BIN" "dist-bin/amst.exe"
            (cd dist-bin && 7z a "${NAME}.zip" amst.exe)
            (cd dist-bin && (sha256sum "${NAME}.zip" || shasum -a 256 "${NAME}.zip") > "${NAME}.zip.sha256")
          else
            cp "$BIN" "dist-bin/amst"
            tar -czf "dist-bin/${NAME}.tar.gz" -C dist-bin amst
            (cd dist-bin && (sha256sum "${NAME}.tar.gz" || shasum -a 256 "${NAME}.tar.gz") > "${NAME}.tar.gz.sha256")
          fi

      # Package the portable GUI binary the same way so the self-updater can
      # fetch `allmystuff-gui-<platform>.<ext>` too. The OS bundles
      # (.deb/.dmg/.msi/...) uploaded above are still the "proper install";
      # this is the lightweight half. Remove the raw binary afterwards so it
      # isn't caught by the upload glob below — only the archives + .sha256
      # ship.
      - name: Package portable GUI binary
        shell: bash
        run: |
          set -e
          BIN=gui/src-tauri/target/${{ matrix.platform.target }}/release/allmystuff-gui
          if [[ "${{ runner.os }}" == "Windows" ]]; then BIN="${BIN}.exe"; fi
          if [[ ! -f "$BIN" ]]; then
            echo "GUI binary not found at $BIN"; exit 1
          fi
          mkdir -p dist-bin
          NAME="allmystuff-gui-${{ matrix.platform.name }}"
          if [[ "${{ runner.os }}" == "Windows" ]]; then
            cp "$BIN" "dist-bin/allmystuff-gui.exe"
            (cd dist-bin && 7z a "${NAME}.zip" allmystuff-gui.exe)
            (cd dist-bin && (sha256sum "${NAME}.zip" || shasum -a 256 "${NAME}.zip") > "${NAME}.zip.sha256")
            rm -f dist-bin/allmystuff-gui.exe
          else
            cp "$BIN" "dist-bin/allmystuff-gui"
            tar -czf "dist-bin/${NAME}.tar.gz" -C dist-bin allmystuff-gui
            (cd dist-bin && (sha256sum "${NAME}.tar.gz" || shasum -a 256 "${NAME}.tar.gz") > "${NAME}.tar.gz.sha256")
            rm -f dist-bin/allmystuff-gui
          fi

      # Package the portable node binary so a headless box can fetch
      # `allmystuff-serve-<platform>.<ext>` (the installer drops it next to
      # the CLI, and `allmystuff serve` / `service` find it there).
      - name: Package portable node binary
        shell: bash
        run: |
          set -e
          BIN=node/target/${{ matrix.platform.target }}/release/allmystuff-serve
          if [[ "${{ runner.os }}" == "Windows" ]]; then BIN="${BIN}.exe"; fi
          if [[ ! -f "$BIN" ]]; then
            echo "node serve binary not found at $BIN"; exit 1
          fi
          mkdir -p dist-bin
          NAME="allmystuff-serve-${{ matrix.platform.name }}"
          if [[ "${{ runner.os }}" == "Windows" ]]; then
            cp "$BIN" "dist-bin/allmystuff-serve.exe"
            (cd dist-bin && 7z a "${NAME}.zip" allmystuff-serve.exe)
            (cd dist-bin && (sha256sum "${NAME}.zip" || shasum -a 256 "${NAME}.zip") > "${NAME}.zip.sha256")
            rm -f dist-bin/allmystuff-serve.exe
          else
            cp "$BIN" "dist-bin/allmystuff-serve"
            tar -czf "dist-bin/${NAME}.tar.gz" -C dist-bin allmystuff-serve
            (cd dist-bin && (sha256sum "${NAME}.tar.gz" || shasum -a 256 "${NAME}.tar.gz") > "${NAME}.tar.gz.sha256")
            rm -f dist-bin/allmystuff-serve
          fi

      # Tag-triggered releases own the release metadata as well as its assets.
      # Keep that lifecycle in one action so a new tag can create/finalize the
      # release normally.
      - uses: softprops/action-gh-release@v2
        if: github.event_name == 'push'
        with:
          tag_name: ${{ inputs.tag || github.ref_name }}
          files: |
            dist-bin/allmystuff-*
            dist-bin/amst-*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # A workflow_dispatch run rebuilds an already-published tag. Upload the
      # replacement assets directly instead of asking action-gh-release to
      # PATCH the existing release metadata; GitHub rejects that integration
      # mutation for a protected release even though its contents token is
      # allowed to replace assets.
      - name: Upload portable artifacts to existing release
        if: github.event_name == 'workflow_dispatch'
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ inputs.tag }}
        run: |
          gh release upload "$TAG" \
            dist-bin/allmystuff-* dist-bin/amst-* \
            --repo "$GITHUB_REPOSITORY" \
            --clobber

  windows-bundles:
    name: Build (windows-x86_64)
    runs-on:
    - self-hosted
    - Windows
    - X64
    - test-fleet
    - windows-native-x64
    timeout-minutes: 180
    env:
      CMAKE_POLICY_VERSION_MINIMUM: '3.5'
      ALLMYSTUFF_RELEASE_PUBKEY: ${{ vars.ALLMYSTUFF_RELEASE_PUBKEY }}
      ALLMYSTUFF_REQUIRE_PINNED_MESH: '1'
    steps:
    - name: Prepare the qualified Windows toolchain
      shell: powershell
      run: '& C:\actions-runner\fleet\release-v1\setup_windows_release.ps1'
    - uses: actions/checkout@v4
      with:
        ref: ${{ inputs.tag || github.ref }}
        path: source
        persist-credentials: 'false'
    - uses: pnpm/action-setup@v4
      with:
        package_json_file: source/gui/package.json
    - uses: actions/setup-node@v4
      with:
        node-version: '20'
    - uses: dtolnay/rust-toolchain@stable
      with:
        targets: x86_64-pc-windows-msvc
    - name: Verify tag matches manifest versions
      shell: bash
      run: |
        set -e
        TAG="${{ inputs.tag || github.ref_name }}"
        EXPECTED="${TAG#v}"
        fail() { echo "::error::$*"; exit 1; }
        got_workspace=$(awk '
          /^\[workspace\.package\]/ {in_pkg=1; next}
          /^\[/ {in_pkg=0}
          in_pkg && /^version =/ {gsub(/[ "]/, "", $0); split($0, a, "="); print a[2]; exit}
        ' Cargo.toml)
        got_gui_cargo=$(awk -F\" '/^version = / {print $2; exit}' gui/src-tauri/Cargo.toml)
        got_gui_pkg=$(node -e "console.log(require('./gui/package.json').version)")
        [[ "$got_workspace" == "$EXPECTED" ]] || fail "Cargo.toml [workspace.package].version is $got_workspace, expected $EXPECTED (tag $TAG). Run 'just release $EXPECTED' to bump."
        [[ "$got_gui_cargo" == "$EXPECTED" ]] || fail "gui/src-tauri/Cargo.toml version is $got_gui_cargo, expected $EXPECTED (tag $TAG). Run 'just release $EXPECTED' to bump."
        [[ "$got_gui_pkg"   == "$EXPECTED" ]] || fail "gui/package.json version is $got_gui_pkg, expected $EXPECTED (tag $TAG). Run 'just release $EXPECTED' to bump."
        echo "Manifest versions match tag $TAG ($EXPECTED)."
      working-directory: source
    - name: Build Windows release packages and required pinned sidecars
      shell: pwsh
      run: |
        $Source='./source'
        $ErrorActionPreference='Stop'
        if($env:COMPUTERNAME -ne 'LAB-WIN-01'){throw 'Expected disposable Windows guest'}
        $volume=Get-Volume -DriveLetter D
        if($volume.FileSystemLabel -ne 'TestFleetCache' -or $volume.SizeRemaining -lt 8GB){throw 'Expected cache volume with at least 8 GiB build headroom'}
        if($env:CARGO_TARGET_DIR){throw 'Unexpected inherited target directory'}
        $root=(Resolve-Path -LiteralPath $Source).Path
        $workspace=(Resolve-Path -LiteralPath $env:GITHUB_WORKSPACE).Path.TrimEnd('\')+'\'
        if(-not $root.StartsWith($workspace,[StringComparison]::OrdinalIgnoreCase)){throw 'Source must be inside this job workspace'}
        Set-Location -LiteralPath $root
        if($env:ALLMYSTUFF_REQUIRE_PINNED_MESH -ne '1' -or $env:ALLMYSTUFF_SKIP_SIDECAR){throw 'Required pinned sidecar must remain enabled'}

        # The reviewed VS layout includes CMake; it is not on a service account's PATH.
        $vswhere='C:\Program Files (x86)\Microsoft Visual Studio\Installer\vswhere.exe'
        $cmakeRoot=@(& $vswhere -products Microsoft.VisualStudio.Product.BuildTools -requires Microsoft.VisualStudio.Component.VC.CMake.Project -property installationPath)
        if($LASTEXITCODE -ne 0 -or $cmakeRoot.Count -ne 1 -or $cmakeRoot[0] -ne 'C:\TestFleetBuildTools'){throw 'Reviewed VS CMake component is not registered'}
        $cmake=Join-Path $cmakeRoot[0] 'Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe'
        if(-not(Test-Path -LiteralPath $cmake -PathType Leaf)){throw 'Registered CMake executable missing'}
        $env:CMAKE=$cmake
        $env:Path=(Split-Path -Parent $cmake)+';'+$env:Path
        & $cmake --version
        if($LASTEXITCODE -ne 0){throw 'Registered CMake runtime failed'}

        $module=([uri]'C:\actions-runner\fleet\release-v1\resources_job.mjs').AbsoluteUri
        $values=& node --input-type=module -e "import {currentBudget,jobEnvironment} from '$module';const b=currentBudget();for(const [k,v] of Object.entries(jobEnvironment(b)))console.log(k+'='+v)"
        if($LASTEXITCODE -ne 0){throw 'Resource allocation failed'}
        foreach($line in $values){$pair=$line -split '=',2;[Environment]::SetEnvironmentVariable($pair[0],$pair[1],'Process')}
        Write-Output ('Compiler workers: '+$env:CARGO_BUILD_JOBS)
        $cache='D:\TestFleetCache\release-targets\allmystuff'
        # MSBuild tracking fails through junctions on this guest. Compile via direct
        # paths; keep source-layout aliases for the original sidecar/artifact lookup.
        # New native cache names avoid failed CMake state containing the old alias path.
        $rootTarget=Join-Path $cache 'root'
        $nodeTarget=Join-Path $cache 'node-native-v2'
        $guiTarget=Join-Path $cache 'gui-native-v2'
        foreach($entry in @(@{relative='target';name='root'},@{relative='node\target';name='node-native-v2'},@{relative='gui\src-tauri\target';name='gui-native-v2'})) {
         $link=Join-Path $root $entry.relative
         if(Test-Path -LiteralPath $link){throw 'Unexpected target in fresh checkout'}
         $target=Join-Path $cache $entry.name
         if(Test-Path -LiteralPath $target){if((Get-Item -LiteralPath $target).Attributes -band [IO.FileAttributes]::ReparsePoint){throw 'Cache target is a reparse point'}}
         New-Item -ItemType Directory -Path $target -Force|Out-Null
         New-Item -ItemType Junction -Path $link -Target $target|Out-Null
        }

        bash ./scripts/sync-pinned-locks.sh --check
        if($LASTEXITCODE -ne 0){throw 'Committed suite pins do not match'}
        pnpm --dir gui install --frozen-lockfile --store-dir D:\TestFleetCache\user\pnpm
        if($LASTEXITCODE -ne 0){throw 'Frozen GUI dependency install failed'}
        foreach($name in @('allmystuff','amst')) {
         cargo build --release --bin $name --target x86_64-pc-windows-msvc --target-dir $rootTarget
         if($LASTEXITCODE -ne 0){throw ('Portable build failed: '+$name)}
        }
        cargo build --release --manifest-path node/Cargo.toml --bin allmystuff-serve --target x86_64-pc-windows-msvc --target-dir $nodeTarget
        if($LASTEXITCODE -ne 0){throw 'Node build failed'}
        $config=Join-Path $env:RUNNER_TEMP 'fleet-allmystuff-config.json'
        [IO.File]::WriteAllText($config,'{"bundle":{"useLocalToolsDir":true}}',[Text.UTF8Encoding]::new($false))
        # Direct CLI selection avoids tauri-action discovering the separate mobile app.
        $env:CARGO_TARGET_DIR=$guiTarget
        try {
         pnpm --dir gui exec tauri build --target x86_64-pc-windows-msvc --bundles nsis --config $config
         if($LASTEXITCODE -ne 0){throw 'Desktop NSIS build failed'}
        } finally {$env:CARGO_TARGET_DIR=$null}

        $output=Join-Path $env:GITHUB_WORKSPACE 'allmystuff-artifacts'
        if(Test-Path -LiteralPath $output){throw 'Unexpected prior qualification artifacts'}
        New-Item -ItemType Directory -Path $output|Out-Null
        $portable=@(
         @{name='allmystuff';path='target\x86_64-pc-windows-msvc\release\allmystuff.exe'},
         @{name='amst';path='target\x86_64-pc-windows-msvc\release\amst.exe'},
         @{name='allmystuff-serve';path='node\target\x86_64-pc-windows-msvc\release\allmystuff-serve.exe'},
         @{name='allmystuff-gui';path='gui\src-tauri\target\x86_64-pc-windows-msvc\release\allmystuff-gui.exe'}
        )
        foreach($item in $portable) {
         $binary=Join-Path $root $item.path
         if(-not(Test-Path -LiteralPath $binary -PathType Leaf)){throw ('Missing release output: '+$item.name)}
         $archive=Join-Path $output ($item.name+'-windows-x86_64.zip')
         Compress-Archive -LiteralPath $binary -DestinationPath $archive
         $sum=(Get-FileHash -LiteralPath $archive -Algorithm SHA256).Hash.ToLowerInvariant()
         [IO.File]::WriteAllText($archive+'.sha256',$sum+'  '+[IO.Path]::GetFileName($archive)+"`n",[Text.UTF8Encoding]::new($false))
        }
        $version=(Get-Content -LiteralPath 'gui/package.json' -Raw|ConvertFrom-Json).version
        $installer=Join-Path $guiTarget ('x86_64-pc-windows-msvc\release\bundle\nsis\AllMyStuff_'+$version+'_x64-setup.exe')
        if(-not(Test-Path -LiteralPath $installer -PathType Leaf)){throw 'Expected current-version NSIS installer'}
        Copy-Item -LiteralPath $installer -Destination $output
        Get-ChildItem -LiteralPath $output -File|Get-FileHash -Algorithm SHA256|Select-Object Path,Hash|ConvertTo-Json
    - uses: actions/upload-artifact@v4
      with:
        name: allmystuff-windows-x64-${{ github.run_id }}-${{ github.run_attempt }}
        path: allmystuff-artifacts/*
        if-no-files-found: error
    - name: Publish Windows installer and portable archives
      uses: softprops/action-gh-release@v2
      if: github.event_name == 'push'
      with:
        tag_name: ${{ inputs.tag || github.ref_name }}
        name: ${{ inputs.tag || github.ref_name }}
        draft: false
        prerelease: false
        fail_on_unmatched_files: true
        files: |
          allmystuff-artifacts/AllMyStuff_*_x64-setup.exe
          allmystuff-artifacts/allmystuff-*.zip*
          allmystuff-artifacts/amst-*.zip*
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: Upload Windows artifacts to the existing release
      if: github.event_name == 'workflow_dispatch'
      shell: pwsh
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        TAG: ${{ inputs.tag }}
      run: |
        $ErrorActionPreference='Stop'
        $assets=@(Get-ChildItem -LiteralPath (Join-Path $env:GITHUB_WORKSPACE 'allmystuff-artifacts') -File|ForEach-Object FullName)
        if($assets.Count -ne 9){throw 'Expected four portable ZIPs with checksums and one NSIS installer'}
        & gh release upload $env:TAG @assets --repo $env:GITHUB_REPOSITORY --clobber
        if($LASTEXITCODE -ne 0){throw 'Existing-release asset upload failed'}

  linux-bundles:
    name: Build (linux-x86_64)
    runs-on:
    - self-hosted
    - Linux
    - X64
    - test-fleet
    - linux-docker-x64
    timeout-minutes: 180
    env:
      CMAKE_POLICY_VERSION_MINIMUM: '3.5'
      ALLMYSTUFF_RELEASE_PUBKEY: ${{ vars.ALLMYSTUFF_RELEASE_PUBKEY }}
      ALLMYSTUFF_REQUIRE_PINNED_MESH: '1'
    steps:
    - name: Fetch the reviewed release tools through the existing source broker
      shell: bash
      run: |
        python3 /opt/test-fleet/actions-runner/fleet-source.py nathanfraske/test-fleet e57459c6a743584c288331dc3bb7107104c1cdb9 fleet
        python3 - <<'PY'
        import hashlib
        from pathlib import Path
        expected = {'controller/release_allmystuff_linux.py': '7b107319ed2b568feb27dca2645d8e50ef0e7eacaada4398baf011ef77744b27', 'controller/release_allmystuff_linux.sh': '2eb176efa27e63106c7a5ec8f4826bbd8620e600638e9af4266ea623ca03b63d', 'controller/images/allmystuff-linux/Dockerfile': 'f7e6239669a1cbc959050566245ad2d73a50a5a2e0704778554ad7ca9d57f5d5', 'controller/images/allmystuff-linux/release-deps.sh': '399fa3892e440270adfe2566f5852fda6ab2ed9420e19d53a70f5a2521c210ef', 'controller/build_linux.py': 'f1f849f1d845f949959c45d31affd574607b9fd73093e27bb8964dd94ab74a8d', 'controller/resource_budget.py': 'a5134a21e8fa7eb039f752fd51975696bade8608dafce2a4cc06b75baf9f6135', 'controller/upload_allmystuff_linux.cjs': '0114506be1287beeb11e45233a1ef6b15ec58316fdce27a13a20a14c05eb00c3'}
        for name, digest in expected.items():
            assert hashlib.sha256((Path('fleet') / name).read_bytes()).hexdigest() == digest, name
        print('Reviewed release tool hashes verified')
        PY
    - uses: actions/checkout@v4
      with:
        ref: ${{ inputs.tag || github.ref }}
        path: source
        persist-credentials: false
      id: source
    - uses: pnpm/action-setup@v4
      with:
        package_json_file: source/gui/package.json
    - uses: actions/setup-node@v4
      with:
        node-version: '20'
    - name: Verify tag matches manifest versions
      shell: bash
      run: |
        set -e
        TAG="${{ inputs.tag || github.ref_name }}"
        EXPECTED="${TAG#v}"
        fail() { echo "::error::$*"; exit 1; }
        got_workspace=$(awk '
          /^\[workspace\.package\]/ {in_pkg=1; next}
          /^\[/ {in_pkg=0}
          in_pkg && /^version =/ {gsub(/[ "]/, "", $0); split($0, a, "="); print a[2]; exit}
        ' Cargo.toml)
        got_gui_cargo=$(awk -F\" '/^version = / {print $2; exit}' gui/src-tauri/Cargo.toml)
        got_gui_pkg=$(node -e "console.log(require('./gui/package.json').version)")
        [[ "$got_workspace" == "$EXPECTED" ]] || fail "Cargo.toml [workspace.package].version is $got_workspace, expected $EXPECTED (tag $TAG). Run 'just release $EXPECTED' to bump."
        [[ "$got_gui_cargo" == "$EXPECTED" ]] || fail "gui/src-tauri/Cargo.toml version is $got_gui_cargo, expected $EXPECTED (tag $TAG). Run 'just release $EXPECTED' to bump."
        [[ "$got_gui_pkg"   == "$EXPECTED" ]] || fail "gui/package.json version is $got_gui_pkg, expected $EXPECTED (tag $TAG). Run 'just release $EXPECTED' to bump."
        echo "Manifest versions match tag $TAG ($EXPECTED)."
      working-directory: source
    - name: Build Linux release packages and required pinned sidecars
      shell: bash
      run: python3 fleet/controller/release_allmystuff_linux.py source
      env:
        FLEET_SOURCE_SHA: ${{ steps.source.outputs.commit }}
    - uses: actions/upload-artifact@v4
      with:
        name: allmystuff-linux-x64-${{ github.run_id }}-${{ github.run_attempt }}
        path: allmystuff-linux/*
        if-no-files-found: error
    - name: Publish Linux bundles and portable archives
      uses: softprops/action-gh-release@v2
      if: github.event_name == 'push'
      with:
        tag_name: ${{ inputs.tag || github.ref_name }}
        name: ${{ inputs.tag || github.ref_name }}
        draft: false
        prerelease: false
        fail_on_unmatched_files: true
        files: |
          allmystuff-linux/AllMyStuff_*_amd64.deb
          allmystuff-linux/AllMyStuff_*_amd64.AppImage
          allmystuff-linux/AllMyStuff-*.x86_64.rpm
          allmystuff-linux/allmystuff-*.tar.gz*
          allmystuff-linux/amst-*.tar.gz*
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: Upload Linux assets to the existing release
      uses: actions/github-script@v7
      if: github.event_name == 'workflow_dispatch'
      env:
        TAG: ${{ inputs.tag }}
      with:
        retries: 0
        script: return await require('./fleet/controller/upload_allmystuff_linux.cjs')({github, context}, process.env.GITHUB_WORKSPACE, process.env.TAG);

  # Detached Ed25519 (minisign) signatures over the portable archives, so the
  # self-updater can prove provenance, not just integrity. Runs once after all
  # platform bundles upload, and is a NO-OP until the maintainer configures the
  # MINISIGN_SECRET_KEY secret (and bakes ALLMYSTUFF_RELEASE_PUBKEY into the
  # shipped build) — see RELEASE-SIGNING.md. Gating means an unconfigured repo
  # still releases exactly as before.
  sign:
    name: Sign release artifacts (minisign)
    needs: [bundles, windows-bundles, linux-bundles]
    runs-on: ubuntu-22.04
    permissions:
      contents: write
    steps:
      - name: Gate on signing key
        id: gate
        env:
          MINISIGN_SECRET_KEY: ${{ secrets.MINISIGN_SECRET_KEY }}
        run: |
          if [ -n "$MINISIGN_SECRET_KEY" ]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "::notice::MINISIGN_SECRET_KEY not configured — skipping signing; artifacts ship SHA-256-only. See RELEASE-SIGNING.md."
          fi

      - name: Install minisign
        if: steps.gate.outputs.enabled == 'true'
        run: |
          set -e
          curl -fsSL -o minisign.tar.gz \
            https://github.com/jedisct1/minisign/releases/download/0.11/minisign-0.11-linux.tar.gz
          tar -xzf minisign.tar.gz
          sudo install -m 0755 minisign-linux/x86_64/minisign /usr/local/bin/minisign

      - name: Download release artifacts
        if: steps.gate.outputs.enabled == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ inputs.tag || github.ref_name }}
        run: |
          set -e
          mkdir -p signing
          gh release download "$TAG" --repo "$GITHUB_REPOSITORY" --dir signing \
            --pattern 'allmystuff-*.tar.gz' --pattern 'allmystuff-*.zip' \
            --pattern 'amst-*.tar.gz' --pattern 'amst-*.zip'

      - name: Sign and upload signatures
        if: steps.gate.outputs.enabled == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          MINISIGN_SECRET_KEY: ${{ secrets.MINISIGN_SECRET_KEY }}
          TAG: ${{ inputs.tag || github.ref_name }}
        run: |
          set -e
          # The minisign secret key is two lines — an "untrusted comment:" line
          # and the base64 key beneath it. Strip CR as we write it: a key pasted
          # or stored with Windows CRLF makes minisign fail to load it ("Error
          # while loading the secret key file") even though the same key signed
          # fine before the secret was last re-entered. Then sanity-check the
          # shape and fail with an actionable message rather than minisign's
          # cryptic one, so a bad secret is obvious instead of shipping a
          # published-but-unsigned release that blocks every client's update.
          printf '%s\n' "$MINISIGN_SECRET_KEY" | tr -d '\r' > signing/minisign.key
          if [ "$(grep -c . signing/minisign.key)" -lt 2 ]; then
            rm -f signing/minisign.key
            echo "::error::MINISIGN_SECRET_KEY is malformed. minisign needs the full TWO lines of minisign.key (the 'untrusted comment:' line and the base64 secret key beneath it), with Unix (LF) line endings and no surrounding quotes. Re-add the secret verbatim and re-run. See RELEASE-SIGNING.md."
            exit 1
          fi
          cd signing
          shopt -s nullglob
          # Password-less CI key (see RELEASE-SIGNING.md): no prompt, </dev/null
          # guards against ever hanging on one.
          for f in allmystuff-*.tar.gz allmystuff-*.zip amst-*.tar.gz amst-*.zip; do
            minisign -S -s minisign.key -m "$f" -x "$f.minisig" </dev/null
          done
          rm -f minisign.key
          gh release upload "$TAG" allmystuff-*.minisig amst-*.minisig --repo "$GITHUB_REPOSITORY" --clobber
